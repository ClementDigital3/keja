{
  "name": "keja-root",
  "version": "1.0.0",
  "description": "Keja root workspace orchestration",
  "scripts": {
    "dev": "npm --prefix keja-frontend run dev",
    "backend": "npm --prefix keja-backend run dev",
    "install:all": "npm install --prefix keja-frontend && npm install --prefix keja-backend"
  }
}
